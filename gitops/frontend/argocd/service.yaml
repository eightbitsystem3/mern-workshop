apiVersion: v1
kind: Service
metadata:
  name: mern-frontend-service
  namespace: mern
spec:
  type: NodePort
  selector:
    app: mern-frontend
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007
